# 2026-08-31 — /work 외주 포트폴리오 Phase 1 (비번보호 숨김 메뉴)

## 배경
외주로 구축한 클라이언트 서비스를 영업 대상에게 보여줄 **비번보호 숨김 메뉴** 신설.
기존 `/showcase`(자체 서비스)는 그대로 유지. 스펙·계획: `docs/prd/work-showcase.md`, `docs/plans/2026-08-31-work-showcase.md`.

## Phase 1 (배포 완료)
- **비번 게이트** — `lib/work/auth.ts` + `pages/api/work/{login,logout}`. **admin과 분리된** 쿠키(`work_session`)·시크릿(`WORK_PASSWORD`). 클라이언트에 admin 권한 노출 안 함.
- `middleware.ts` matcher에 `/work/:path*` 추가 → 미인증 시 `/work/login` 리다이렉트.
- `pages/work/login.tsx`(noindex) + `pages/_meta.ts` `work` 숨김 폴더 등록.
- `data/work/projects.ts`(`WorkProject` 모델) + `components/work/WorkCard`(Grid).
- `pages/work/index.mdx` — 디어유 카드 1건 end-to-end.
- `.gitignore`에 `!data/work/` 예외 추가(`data/*` 규칙에 걸려 있던 것).

## 검증
- 로컬 dev: `/work` 307 / `/work/login` 200 / login 401·200 / 쿠키 후 `/work` 200 + 디어유 렌더.
- 프로덕션(배포 후): `./scripts/check-routes.sh` **131개 fail=0**(기존 화면 유실 0), `/work` 게이트 동일 동작 확인.
- `WORK_PASSWORD` = Vercel 프로덕션 env(Sensitive) 설정 완료. **레포 미커밋.**

## 남음 (Phase 2/3)
- Phase 2: 나머지 10건(브릿지세븐/b7·블루망고·에어프레미아·닥터리본·etude×4·어스오브제·타로몬) 데이터 + **실 스크린샷**(디어유 커버는 현재 플레이스홀더).
- Phase 3: 대표 2~3건 상세 케이스(`pages/work/[slug].tsx`).
- 컨셉은 **외주 모음** 고정(타로몬 등도 외주로 취급·표기).
