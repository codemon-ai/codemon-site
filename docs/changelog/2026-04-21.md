# 2026-04-21 — Airpremia Level 1 서베이 추가

## 변경 요약
에어프레미아 사내 Claude 실무 교육 Level 1(airpremia 레포 `/slides/session-5`)이 이미 안내하고 있는 설문 URL `https://codemon.ai/survey/airpremia-lv1`에 실제 동작하는 페이지를 붙였다. 기존 서베이 구조(SurveyForm + Supabase `survey_responses` + 어드민 집계)를 그대로 재사용한다.

## 파일 수정
- `lib/survey.ts` — `isValidLectureId` 를 정규식(`/^lecture-[a-z0-9-]+$/`) 기반에서 `SUPPORTED_LECTURE_IDS` 화이트리스트 기반으로 전환. 앞으로 서베이 추가 시 이 배열만 갱신하면 된다.
- `pages/survey/[lectureId].tsx` — `LECTURES` 맵에 `'airpremia-lv1': 'Airpremia Level 1 · Claude 생태계 실무 교육'` 추가.
- `pages/api/survey/submit.ts` — `LECTURE_TITLES` 맵에 동일 항목 추가 (Slack 알림 강의명).
- `pages/admin/surveys.tsx` — 어드민 필터 탭에 `Airpremia Lv1` 추가.

## 로컬 검증 (playwright-cli)
- `/survey/airpremia-lv1` → "강의 설문 — Airpremia Level 1 · Claude 생태계 실무 교육" 타이틀, 7필드 폼 + QR 렌더링 OK
- `/survey/lecture-agency-ai` 등 기존 3개 URL 정상 동작 보존
- `/survey/nonexistent-id` → "유효하지 않은 설문입니다" 차단 유지

## 비작업 (다음 이터레이션)
- 에어프레미아 전용 커스텀 질문(Cowork 예약 이해도, Dispatch 활용 의지 등 session-5 슬라이드 19의 "익명·선택형 10문항")은 현 스키마 범위 밖 — 수집 시 `gains`/`questions` 자유기술로 흡수. 필요 시 폼/DB 스키마 확장 별도 작업.
- airpremia 레포 수정 없음 (session-5.html URL 이미 일치, QR SVG 존재).
