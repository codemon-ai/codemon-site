# 2026-07-24 — 열린 PR 전량 머지 + Supabase 마이그레이션 완료

## PR #27~#31 머지
2026-07-21 기반 정비의 후속. 열린 PR을 `#28 → #27 → #29 → #30` 순서로 머지(리허설대로 충돌 0),
정리 결과 원장(#31)까지 반영. `origin/main`: `ffbe929` → `0e0dedf`. 머지 후 빌드 통과(정적 페이지 223개).
어떤 화면·라우트 파일도 삭제되지 않음.

## Supabase 마이그레이션 완료 (설문 4문항)
PR #30의 설문 확장(`learnings`/`follow_along`/`would_help`/`improvements`)에 필요한
`survey_responses` 컬럼 4개를 **프로덕션에 적용 + 검증**.

- 방법: `supabase db query --linked` (CLI v2.109 via npx, Management API 경유). 저장된 액세스 토큰 사용,
  원격 DB 비밀번호 불필요. 프로젝트 ref `uhynpqyrpquueareqync`.
- `supabase db push`는 원격 마이그레이션 이력의 8자리 phantom 버전(`20260228`) 때문에 실패했고,
  CLI가 권한 오류 시 안내한 `migration repair`(원격 이력 변경)는 파괴적 명령으로 차단됨.
  → 이력 검증을 거치지 않는 `db query`로 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 단일 실행.
- 검증: `information_schema.columns` 조회로 4개 컬럼 모두 `text` 생성 확인 (before 12컬럼 → after 16컬럼).
- 결과: 설문 4문항 기능 배포 안전(insert 성공 → 어드민 노출).

## 브랜치 정리
- 안전 확인된 원격 브랜치 10개 + 리베이스 백업 1개 삭제 **대기**(복원용 SHA는 `docs/wiki/branches.md`).
  실제 원격 삭제는 파괴적 원격 작업으로 harness 차단 → 코드몬 승인 후 진행.
- `feature/partner-admin`·`vercel/...analytics`는 main보다 뒤처진 **머지 금지** 브랜치로 명시.

## 남은 것
- 원격 브랜치 10개 삭제(선택, 무해) — 승인 시 진행.
