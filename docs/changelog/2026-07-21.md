# 2026-07-21 — 기반 정비: 화면 유실 방지 체계 구축

## 배경
브랜치 관리 실수로 **예전 화면이 프로덕션에서 사라지는 사고**가 반복됐다.
기능 개선을 이어가기 전에 "무엇이 살아있어야 하는가"를 문서로 고정하고 기계로 검증할 수 있게 정비했다.

## 전수 조사 결과

### 프로덕션 라우트 (2026-07-21 전수 점검)
- 공개 페이지 **129** → 121×200 + 8×3xx(의도된 리다이렉트: `/admin*` 로그인 307, `/partner/survey/*` → `/survey/*` 308)
- 비공개 `/p/`·`/en/p/` **65** → 전부 200
- 정적 HTML/PDF **15** → 전부 200
- **합계 209개, 현재 유실 0건** — 즉 지금 프로덕션은 `main`과 일치한다.

### 브랜치 · PR (20개 브랜치 / PR 1~28)
- 열린 PR 2건: **#27** `docs/code-wiki`(머지 가능) / **#28** `docs/partner-lectures-handoff`(⚠️ `docs/INDEX.md`·`changelog/2026-06-27*` 충돌 — rebase 필요)
- ⚠️ **미머지 상태로 닫힌 PR #8 (`feat/about-education`) 발견** — `main`에 없는 실제 작업물 2커밋:
  ① `/about` 학력 섹션(`components/Career.tsx`) ② 설문 4문항 확장(`learnings`/`follow_along`/`would_help`/`improvements`)
  → 되살림/폐기 **결정 대기**. 결정 전 브랜치 삭제 금지.
- 삭제해도 안전한 브랜치 15개 확인(내용 100% main 반영). `feature/partner-admin`은 `ahead=3`으로 보이지만
  실제 차이는 main 쪽이 더 최신인 `lib/survey.ts`·`docs/INDEX.md` 뿐 → 안전.
- `vercel/vercel-web-analytics-to-nextjs-que93t`(PR #2 닫힘)도 `@vercel/analytics` 이미 main 반영 → 안전.

### 사고 원인 규명 (3종)
1. 배포가 `vercel deploy --prebuilt --prod` = **git이 아닌 로컬 디스크 스냅샷**. 워크트리 3개 중 뒤처진 트리에서 배포 → 타 작업 덮어씀.
2. 머지 안 된 PR을 그냥 닫음 (PR #8).
3. `public/partner/*.html`이 `pages/partner/` 라우트에 가려 404 (2026-06-23, PR #20→#21로 `/slides/` 이전 복구).

## 추가/수정 파일
- **신규** `docs/wiki/route-inventory.md` — 살아있어야 할 화면 209개 기준선 + 섹션별 구성 + 점검법
- **신규** `docs/wiki/branches.md` — 브랜치·PR 원장(상태/미반영 커밋/삭제 안전 여부) + 운영 규칙 6개
- **신규** `scripts/check-routes.sh` — 전 라우트 HTTP 점검, 200/3xx 외 발견 시 exit 1. `--all`로 비공개+정적자산까지, `BASE=` 로 프리뷰 검증
- `CLAUDE.md` — 최상단 "🚨 화면 유실 방지" 규칙표 + 작업 시작 전 체크 2개 + DoD 2개 추가
- `docs/wiki/decisions.md` — **ADR-004** 화면 유실 방지(라우트 인벤토리 + 브랜치 원장) 추가
- `docs/INDEX.md` — 라우팅 테이블 2행, 문서 트리, 최근 변경 갱신

## 검증
- `./scripts/check-routes.sh --all` → checked=209, fail=0
- 브랜치 판정은 `git log origin/main..origin/<branch>` + `git diff` + main 코드 grep으로 교차 확인

## 후속 작업 (같은 날)

### 유실 작업물 복원 — PR #30
닫힌 PR #8(`feat/about-education`)의 작업물을 `origin/main` 위로 **체리픽하여 복원**
(`feat/about-education-revive`, PR #30).

- `/about` 학력 섹션(`components/Career.tsx`) + 설문 공통 4문항(`learnings`/`follow_along`/`would_help`/`improvements`)
- 충돌은 `docs/INDEX.md` 최근변경 1건뿐 → 날짜순 정리로 해결
- `lib/survey.ts`는 그 사이 main이 화이트리스트(`SUPPORTED_LECTURE_IDS`)로 바뀐 상태 → **main 최신 구조 유지** 확인
- `npm run build` 통과, main 대비 순수 추가 diff(회귀 없음)
- ⚠️ 머지/배포 전 **Supabase `survey_responses` 컬럼 4개 마이그레이션 필수.** 안 하면 insert 실패로
  듀얼 라이트가 Blob으로만 떨어져 어드민에서 응답이 안 보인다 (원본 changelog의 "값만 누락" 서술을 정정)

### 툴체인 경로 드리프트 수정
문서에 적힌 빌드/배포 명령이 **현재 머신에서 실패**하는 것을 발견 — `~/.nvm` 디렉터리 자체가 없어졌고
node는 `~/.local/bin`(hermes/fnm, v22.23.0)으로 이전됐다. `npm: command not found`.

- `CLAUDE.md`, `docs/wiki/deployment.md`의 배포 명령을 현재 동작하는 `CLEAN` PATH로 교체
- 배포 절차에 "0. 어느 트리에서 배포하는지 확인" + "4. `check-routes.sh`" 단계 추가
- 배포 전/후 체크리스트 갱신

## 다음 액션 (코드몬 결정 필요)
1. PR #29(이 문서) 머지
2. PR #27 머지
3. PR #28 rebase 후 머지
4. Supabase 마이그레이션 실행 → PR #30 머지
5. 위 처리 후 안전 브랜치 15개 + `feat/about-education` 원본 원격 삭제

> #27~#30이 모두 `docs/INDEX.md`를 건드린다. 하나씩 머지하고 다음 브랜치는 `git rebase origin/main`.
