# 2026-04-22

## About 페이지 학력 섹션 추가
- `components/Career.tsx` — Career 타임라인 하단에 "학력" 서브섹션 추가 (GraduationCap 아이콘 + 보라색 포인트 유지).
  - 연세대학교 공학대학원 (재학)
  - 명지대학교 컴퓨터공학과 (졸업)

### 검증
- 로컬 `http://localhost:3010/about` 렌더링 확인 (playwright-cli snapshot).
- 기존 3단 타임라인(대기업 10년 → 스타트업 10년 → AI/AX Engineer) 레이아웃 보존.

## 설문 문항 4개 추가 (learnings / follow_along / would_help / improvements)

모든 `/survey/[lectureId]` 페이지에 공통 4문항을 추가. 전 강의 공유 `SurveyForm` 컴포넌트 한 곳만 바꿔도 반영됨.

### 추가된 문항 (모두 선택 입력, textarea)
- 본인이 배운 것들 → `learnings`
- 어디까지 따라 갔는지 → `follow_along`
- 어떤 것이 있으면 도움이 될지 → `would_help`
- 개선이 되었으면 하는 부분 → `improvements`

기존 `gains`(강의에서 얻은 점) / `questions`(더 궁금한 점)는 하위 호환 위해 유지.

### 수정 파일
- `components/SurveyForm.tsx` — state 4개 + textarea UI 4개 + submit body 확장
- `pages/api/survey/submit.ts` — request body 파싱 + `saveSurveyResponse` 호출 + Slack 알림에 새 항목 포함
- `lib/survey.ts` — `SurveyResponse` 타입 확장, Supabase insert 칼럼 추가
- `lib/admin/types.ts` — 어드민 타입 확장 (nullable: 기존 데이터 호환)
- `components/admin/SurveyTable.tsx` — 확장 패널에 4개 항목 표시
- `lib/admin/surveys.ts` — CSV export select/header/row에 새 칼럼 포함, `escape()`가 `null` 허용

### Supabase 마이그레이션 (수동 실행 필요)

Supabase SQL Editor에서 실행:

```sql
ALTER TABLE survey_responses
  ADD COLUMN IF NOT EXISTS learnings text,
  ADD COLUMN IF NOT EXISTS follow_along text,
  ADD COLUMN IF NOT EXISTS would_help text,
  ADD COLUMN IF NOT EXISTS improvements text;
```

마이그레이션 전에도 insert는 성공(선택 필드)하지만 값은 DB에 기록되지 않음. 배포 전 필수.
