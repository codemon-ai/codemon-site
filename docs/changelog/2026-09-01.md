# 2026-09-01 — /work 보강 (3건 추가 + 2열 + OG 교체)

## 변경
- **카드 3건 추가** → 총 14건:
  - 하이테커 (Claude 실무 VOD 강의, 운영 중)
  - 달러시그널 (AI 환율 예측·시그널, 운영 중) — showcase에서 가져옴
  - **살리타** (침 코르티솔 측정 서비스, **개발중**) — 아이디어 단계 컨셉으로 랜딩 1p 제작 후 OG로 사용
- **그리드 3열 → 2열** (`WorkGrid`)
- **OG(커버) 스크린샷 교체/신규** (playwright, 일부 basic auth):
  - 블루망고 ← `bluemango-v2-web.vercel.app`
  - 에어프레미아 ← `airpremia.vercel.app/v4` (basic auth) — 플레이스홀더→실캡처
  - 하이테커 ← `hightecher-lectures.vercel.app/v2` (basic auth)
  - 살리타 ← 자체 제작 랜딩(`salitar/docs/landing-v1.html`) 캡처
  - 달러시그널 → 라이브 응답이 비어(0바이트) **플레이스홀더**로 대체(교체 예정)

## 검증
- `npm run build` 통과. 배포 후 `check-routes` 131 fail=0(기존 화면 유실 0), `/work` 14건 전부 렌더 + 2열 확인.

## 남음 (코드몬 확인)
- 달러시그널·디어유·브릿지세븐·닥터리본 실 스크린샷 교체
- 살리타 랜딩은 스크린샷 전용(미배포). 실제 제품 진행 시 별도.

## 추가 (2026-09-01) — 파인클럽(FineClub)
- 카드 1건 추가 → 총 **15건**. 반려동물 라이프스타일 커머스 MVP(쇼핑몰+FineMate 앱+fineclub-admin), **완료(done)·MVP** 표기.
- fineclub.co.kr 이미지 4장(메인·룩북·매거진·이벤트) → 상세 페이지 `/work/fineclub` 갤러리.
- 소스: `codemon-ai/FineMate`, `codemon-ai/fineclub-admin`, `fineclub.co.kr`.
- 검증: check-routes 131 fail=0, 상세 게이트(307/200)·갤러리 4장 확인.

## 추가 (2026-09-01) — 베르셀 3건
- 카드 3건 추가 → 총 **18건**:
  - **파스칼(gen-grade)** — 독서논술 AI 성장보고서 생성기(선생님 로그인→관리자 대시보드 캡처)
  - **LG 이벤트 추첨(lg-lucky-draw)** — 이벤트 럭키드로우 웹앱 (완료)
  - **머니몬(moneymon)** — 종합 투자 분석 시스템(Google/Kakao OAuth라 로그인 화면 캡처)
- 검증: check-routes 131 fail=0, 3건 렌더 확인.
- 라이트 테마 토글은 구현 대신 **이슈 #59**로 저장(스파이크 결과·방식 A/B 포함).

## 버그 수정 — 라이트 모드 글씨 반전 (PR #62)
- 증상: 라이트 모드에서 `text-foreground` 글씨가 안 보임(흰 글씨/흰 배경).
- 원인: `tailwind.config`의 `foreground:'#fafafa'`·`background:'#0a0a0a'`가 **고정색**(테마 무반응).
- 수정: `styles/globals.css`에 `:root`(라이트)/`.dark`(다크) CSS 변수(rgb 트리플릿) 정의 + `tailwind.config`를 `rgb(var(--...) / <alpha-value>)`로 → `text-foreground/xx` 투명도까지 양 테마 동작.
- 테스트: `/about` 라이트=글씨 보임 / 다크=기존 동일(무회귀) 스크린샷 대조 + 프로덕션 재확인. check-routes 131 fail=0.
- 관련 이슈 #59(토글). 토글 UI·login.tsx 라이트 대응은 미결.
