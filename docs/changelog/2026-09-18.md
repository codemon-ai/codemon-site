# 2026-09-18 — `/webinar` 사전 준비 가이드 신설

## 변경
- **`/webinar` 페이지 신설** (`pages/webinar/index.mdx` + `_meta.ts`, 루트 `_meta.ts`에 hidden folder 추가).
  - 하이테커 × 코드몬 "Claude 바이브 코딩 핸즈온" 웨비나(60분)의 **사전 준비 정본**. 자료 본체(슬라이드·핸드북·프롬프트)는 `high-techer/webinar-vibe-coding/`에 있고, 핸드북은 이 페이지로 링크한다.
  - 내용: 준비 체크리스트 8항목 → Claude Code 설치(mac/win 탭) → Node·Vercel CLI → 플러그인 2개(`superpowers@claude-plugins-official`, `vercel@claude-plugins-official`) → `/mcp` Vercel 인증 → Playwright MCP 등록 → **Orca 설치(선택, brew cask/다운로드 + 첫 실행 3단계)** → 실습 폴더 + P0 환경 점검 프롬프트 → 준비할 내용 → 트러블슈팅 표 → 당일 흐름 표.
  - Nextra `Callout`·`Steps`·`Tabs` 사용. 사이드바 off, TOC on, 상단 메뉴 비노출(hidden).
- 사실 확인 2026-09-18: Vercel 플러그인·superpowers 마켓플레이스(로컬 설치본 `~/.claude/plugins/installed_plugins.json`), Claude Code 아티팩트·Chrome 공식 문서, Orca 설치 명령(`stablyai/orca` README.ko).

## 검증
- `npm run build` 통과. `next start`로 `/webinar` 200, og:title·본문(Orca brew 명령 포함) 렌더 확인.
- 배포 후 `./scripts/check-routes.sh` — 결과는 아래 "배포" 항목.

## 배포
- 2026-09-18 `vercel build --prod` → `vercel deploy --prebuilt --prod` (main `1e1ddc4`), dpl_Ac6uMf5HXinbNfuLXuDh9bhkRDCf READY → https://codemon.ai
- `check-routes.sh`: checked=132 fail=0. `https://codemon.ai/webinar` 200, 본문 렌더 확인.

## 참고
- 웨비나 자료 PR: `codemon-ai/high-techer` #53(초판), #54(핸즈온 재구성).
- `<title>`이 SSR HTML에서 `GitHub`로 나오는 현상은 전 페이지 공통(기존) — 이번 범위 밖, `issues.md` 후보.
