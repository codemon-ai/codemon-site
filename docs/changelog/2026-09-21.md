# 2026-09-21 — `/webinar` 허브화 + 참가자 핸드북 서빙

## 변경
- `/webinar` 구조: **허브**(`index.mdx`, 사전 준비·핸드북 카드 + 60분 흐름 + 제공 파일) / **`/webinar/prep`**(기존 사전 준비, `index.mdx`→`prep.mdx` 이동) / **`/webinar/handbook`**(신설, 참가자 핸드북 MDX 전환).
- 핸드북: 진행표·8단계 손동작·프롬프트 12개(코드 블록, 복사 버튼)·트러블슈팅·완성 체크리스트·Supabase 부록·출처. 원본은 `high-techer/webinar-vibe-coding/assets/handbook-body.html`, 내용 동일하게 MDX로 옮김(checkbox localStorage → 정적 태스크 리스트).
- 정적 파일 `public/files/webinar/`: `demo-static/{index.html,app.js}`(완성 예제), `prompt-pack.md`, `templates/{portfolio-check.SKILL.md, research-example.md, research-template.md, plan-template.md, supabase-demo.sql}`. `public/webinar/`가 아니라 `public/files/`에 둔 이유: `pages/webinar/` 라우트에 가려 404 나는 기존 이슈(CLAUDE.md `public/partner/` 주의) 회피.
- `pages/webinar/_meta.ts` 3페이지 사이드바(개요 / 1·사전 준비 / 2·핸드북), 루트 `_meta.ts`의 webinar 폴더 `sidebar:false` 제거. 상단 메뉴는 계속 hidden.

## 검증
- `npm run build` 통과. `next start`로 `/webinar`, `/webinar/prep`, `/webinar/handbook` + 정적 파일 5종 200.
- 배포 후 `check-routes.sh` — 아래 기입.

## 배포
- 2026-09-21 main `ee30c04` → dpl_CLJvL83Xzu3J1aEkawjScGazfQpk READY → https://codemon.ai (첫 `vercel deploy --prebuilt` 1회 error 후 재시도 성공 — 09-18에도 동일, 재현 시 issues.md 기록)
- `check-routes.sh` 134/0. `/webinar`·`/webinar/prep`·`/webinar/handbook` + `/files/webinar/*` 200.
