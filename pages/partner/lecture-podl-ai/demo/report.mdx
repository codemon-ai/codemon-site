---
title: "Demo 2. 매출 데이터 일일 리포트"
description: "3채널 매출 데이터를 분석해서 일일 리포트를 자동 생성합니다"
---

import { DemoShell } from '../../../../components/demo/DemoShell'
import { DataPreview, formatWon } from '../../../../components/demo/DataPreview'
import sales from '../../../../data/demo/sales.json'

export const columns = [
  { key: 'date', label: '날짜' },
  { key: '쿠팡', label: '쿠팡', format: formatWon },
  { key: '자사몰', label: '자사몰', format: formatWon },
  { key: '올리브영', label: '올리브영', format: formatWon },
]

export const prompt = `다음은 보들(BO:DL) K-뷰티 브랜드의 최근 7일 채널별 매출 데이터입니다.

일일 매출 리포트를 작성해주세요.

요구사항:
1. 오늘(${sales.daily[sales.daily.length - 1].date}) 매출 요약 (총액, 채널별)
2. 전일 대비 증감률
3. 주간 트렌드 분석 (성장/하락 채널)
4. 이상치 감지 (평소 대비 비정상적 변동)
5. 슬랙 메시지 포맷으로 출력 (이모지 포함)

매출 데이터:
${JSON.stringify(sales.daily, null, 2)}

제품별 주간 매출:
${JSON.stringify(sales.topProducts, null, 2)}`

<DemoShell
  title="매출 데이터 일일 리포트"
  demoNumber={2}
  description="흩어진 채널 매출을 통합 분석하고 슬랙 리포트를 자동 생성합니다"
  defaultPrompt={prompt}
  systemPrompt="당신은 보들(BO:DL) K-뷰티 브랜드의 데이터 분석가입니다. 매출 데이터를 분석하여 경영진이 바로 이해할 수 있는 일일 리포트를 작성합니다. 핵심 인사이트를 먼저 제시하고, 상세 분석을 뒤에 덧붙이세요."
  dataPreview={<DataPreview data={sales.daily} columns={columns} />}
/>
